name: Deploy with SemVer Enforcement

on:
  push:
    branches:
      - main  # Deploys to Dev on push
    tags:
      - 'v*'  # Matches all tags (vX.Y.Z, vX.Y.Z-test, etc.)

jobs:
  deploy:
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.set_env.outputs.environment }}  # Passing environment as output
    steps:
      - name: Determine Environment
        id: set_env
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "environment=dev" >> $GITHUB_ENV
            echo "environment=dev" >> $GITHUB_OUTPUT
          elif [[ "${{ github.ref }}" == refs/tags/v*-test* ]]; then
            echo "environment=test" >> $GITHUB_ENV
            echo "environment=test" >> $GITHUB_OUTPUT
          else
            echo "environment=prod" >> $GITHUB_ENV
            echo "environment=prod" >> $GITHUB_OUTPUT
          fi

      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Deploy
        run: |
          echo "🚀 Deploying to ${{ steps.set_env.outputs.environment }}"
          echo "Using environment: ${{ vars.ENVIRONMENT }}"

  cleanup:
    runs-on: ubuntu-latest
    needs: deploy  # Ensures cleanup runs after deploy
    environment: ${{ needs.deploy.outputs.environment }}
    env:
      ENVIRONMENT: ${{ needs.deploy.outputs.environment }}
    steps:
      - name: Cleanup Resources
        run: |
          echo "🧹 Cleaning up resources in $ENVIRONMENT environment"
          echo "using ${{ vars.ENVIRONMENT }}"
